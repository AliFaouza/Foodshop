{% extends 'base.html.twig' %}

{% block title %}Hello ProduitController!{% endblock %}

{% block body %}
<center>
    <table class="table table-hover">
        <h1>Liste des produits</h1>
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Prix</th>
            <th>Descritpion</th>
            <th>Category</th>
            <th>Action</th>
        </tr>
        {% for produit in produits %}
        <tr>
            <th scope="row">{{ produit.id }}</th>
            <td>{{ produit.Nom}}</td>
            <td>{{ produit.Prix}}€</td>
            <td>{{ produit.Description}}</td>
            <td>{{ produit.Category.Nom}}</td>
            
            <td>
                <a href="{{path('app_produit_delete',{id: produit.id})}}">supprimer</a>
               
                <a href="{{path('app_produit_edit',{id: produit.id})}}">Editer</a>
            </td>
        </tr>
        {% endfor %} 
    </table>
    <div>
        <a href="{{path('app_produit_new')}}" class="btn btn-success">Ajouter un produit</a>
    </div>
</center>

  

    
{% endblock %}
