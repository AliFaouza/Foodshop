<nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid">
          <a class="navbar-brand" href="{{path('app_welcom')}}">Foodshop</a>
           <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item dropdown">
                    {% if is_granted('ROLE_ADMIN') %}

                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                      Admin
                    </a>
                    <ul class="dropdown-menu">
                      <li><a class="dropdown-item" href="{{path('app_admin_category')}}">Categories</a></li>
                      <li><a class="dropdown-item" href="{{path('app_produit')}}">Produits</a></li>
                      <li><a class="dropdown-item" href="{{path('app_admin')}}">Users</a></li>
                    </ul>
                  </li>
                  {% endif %}
                  <li><a  href="{{path('app_register')}}">S'inscrire</a></li>
                  <li><a  href="{{path('app_login')}}">S'identifier</a></li>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li><a  href="{{path('app_logout')}}">Deconnexion</a></li>
                    {% endif %}
                    
               
              </ul>
          </div>
            <div class="border border-raduis">
              <input type="search " placeholder="Search"> <i class="fas fa-search"></i>
            </div>
          </ul>
      </div>
  </nav>