{% extends 'base.html.twig' %}

{% block title %}Hello AdminCategoryController!{% endblock %}

{% block body %}
  <center>
    <table class="table table-hover">
        <h1>Liste des catégories</h1>
        <tr>
            <th>ID</th>
            <th> Nom </th>
            <th>Action</th>
        </tr>
        {% for category in categories %}
        <tr>
           
            <th scope="row">{{ category.id }}</th>
            <td>{{category.Nom}}</td> 
            <td>
                <a href="{{path('app_admin_category_delete', {id: category.id})}}">supprimer</a>
                <a href="{{path('app_admin_category_edit', {id: category.id})}}">Editer</a>
            </td>
        </tr>
        {% endfor %}
    </table>
    
    <div>
        <a href="{{path('app_admin_category_new')}}" class="btn btn-success">Ajouter une categorie</a>
    </div>
    
  </center>

{% endblock %}
